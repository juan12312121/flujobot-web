/** Producción (Vercel): la API vive en Render. Si el servicio de Render cambia de nombre, cambia esta URL. */
export const environment = {
  produccion: true,
  apiUrl: 'https://flujobot-api.onrender.com',
};
